/*
#include "asmbl.h"
struct opcode_s
{
    char *name;
    int val;
} opcode_t;

typedef struct opcode_t OPCODE_T;

OPCODE_T mov; /* cmp, add, sub, not, clr, lea, inc, dec, jmp, bne, red, prn, jsr, rts, hlt */